---
import Layout from "@/layouts/Layout.astro"
import Header from "@/components/layout/Header.astro"
import Footer from "@/components/layout/Footer.astro"
import Button from "@/components/ui/Button.astro"
import Card from "@/components/ui/Card.astro"
import FadeInSection from "@/components/animations/FadeInSection.astro"
import SlideInImage from "@/components/animations/SlideInImage.astro"
import StaggerText from "@/components/animations/StaggerText.astro"
import ParallaxContainer from "@/components/animations/ParallaxContainer.astro"
import artistsData from "@/data/artists.json"

const { artists } = artistsData;
const artist = artists.find(a => a.slug === 'david');

if (!artist) {
  return Astro.redirect('/artistas');
}
---

<Layout 
  title={`${artist.name} - Artista de Tatuajes | Cuba Tattoo Studio`}
  description={`Conoce a ${artist.name}, especialista en ${artist.specialties.join(', ')}. ${artist.bio.substring(0, 150)}...`}
>
  <Header />
  
  <main class="pt-20 bg-black min-h-screen">
    <!-- Hero Section Cinematográfico -->
    <ParallaxContainer speed={0.2} intensity="high" class="py-24 bg-gradient-to-b from-black via-gray-900 to-black relative overflow-hidden">
      <section class="max-w-7xl mx-auto px-6">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
          <!-- Imagen del Artista -->
          <SlideInImage 
            direction="left" 
            delay={0.3} 
            intensity="high" 
            parallax={true}
            class="aspect-[4/5] overflow-hidden relative"
          >
            <img 
              src={`https://trae-api-us.mchost.guru/api/ide/v1/text_to_image?prompt=cinematic%20professional%20tattoo%20artist%20${artist.name.toLowerCase()}%20portrait%20black%20and%20white%20dramatic%20studio%20lighting%20confident%20pose%20detailed%20moody&image_size=portrait_4_3`}
              alt={`${artist.name} - Artista de tatuajes especializado en ${artist.specialties.join(', ')}`}
              class="w-full h-full object-cover"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-black/30 to-transparent"></div>
          </SlideInImage>
          
          <!-- Información del Artista -->
          <div class="space-y-8">
            <FadeInSection delay={0.6} intensity="high">
              <StaggerText 
                tag="h1" 
                text={artist.name.toUpperCase()}
                splitBy="chars"
                delay={0.8}
                stagger={0.05}
                intensity="high"
                class="text-6xl md:text-8xl font-heading text-white uppercase tracking-wider mb-6"
              />
              <div class="w-32 h-1 bg-white mb-8 transform origin-left"></div>
            </FadeInSection>
            
            <!-- Especialidades -->
            <FadeInSection delay={1.2} intensity="medium">
              <h2 class="text-sm font-body text-gray-400 uppercase tracking-wider mb-4">
                Especialidades
              </h2>
              <div class="flex flex-wrap gap-4">
                {artist.specialties.map((specialty, index) => (
                  <FadeInSection delay={1.4 + (index * 0.1)} intensity="medium">
                    <span class="px-6 py-3 bg-gray-800 text-white text-sm font-body uppercase tracking-wider border border-gray-700 hover:bg-gray-700 transition-all duration-300">
                      {specialty}
                    </span>
                  </FadeInSection>
                ))}
              </div>
            </FadeInSection>
              
            <!-- Experiencia -->
            <FadeInSection delay={1.8} intensity="medium">
              <h2 class="text-sm font-body text-gray-400 uppercase tracking-wider mb-4">
                Experiencia
              </h2>
              <p class="text-white font-body text-xl leading-relaxed">
                {artist.experience}
              </p>
            </FadeInSection>
            
            <!-- Contacto -->
            <FadeInSection delay={2.0} intensity="medium">
              <h2 class="text-sm font-body text-gray-400 uppercase tracking-wider mb-4">
                Contacto
              </h2>
              <div class="space-y-3">
                <a 
                  href={`https://instagram.com/${artist.contact.instagram.replace('@', '')}`}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="block text-white hover:text-gray-300 transition-all duration-300 font-body transform hover:translate-x-2"
                >
                  {artist.contact.instagram}
                </a>
                <a 
                  href={`mailto:${artist.contact.email}`}
                  class="block text-white hover:text-gray-300 transition-all duration-300 font-body transform hover:translate-x-2"
                >
                  {artist.contact.email}
                </a>
              </div>
            </FadeInSection>
            
            <!-- CTA -->
            <FadeInSection delay={2.2} intensity="high">
              <div class="flex flex-col sm:flex-row gap-6 pt-6">
                <Button 
                  href={`/reservas?artist=${artist.slug}`} 
                  variant="primary" 
                  size="lg"
                  class="transform hover:scale-105 transition-all duration-300 shadow-2xl"
                >
                  Reservar con {artist.name}
                </Button>
                <Button 
                  href="/portfolio" 
                  variant="outline" 
                  size="lg"
                  class="transform hover:scale-105 transition-all duration-300"
                >
                  Ver Portfolio Completo
                </Button>
              </div>
            </FadeInSection>
          </div>
        </div>
        
        <!-- Elementos decorativos cinematográficos -->
        <div class="absolute top-12 left-12 w-20 h-20 border-l-2 border-t-2 border-white opacity-10"></div>
        <div class="absolute bottom-12 right-12 w-20 h-20 border-r-2 border-b-2 border-white opacity-10"></div>
      </section>
    </ParallaxContainer>

    <!-- Biografía Cinematográfica -->
    <FadeInSection delay={0.3} intensity="high" class="py-24 bg-gradient-to-b from-black to-gray-900">
      <section class="max-w-5xl mx-auto px-6">
        <div class="text-center">
          <StaggerText 
            tag="h2" 
            text={`SOBRE ${artist.name.toUpperCase()}`}
            splitBy="words"
            delay={0.5}
            stagger={0.1}
            intensity="high"
            class="text-5xl md:text-6xl font-heading text-white uppercase tracking-wider mb-12"
          />
          
          <FadeInSection delay={1.0} intensity="medium">
            <div class="max-w-3xl mx-auto">
              <p class="text-gray-300 font-body text-xl leading-relaxed">
                {artist.bio}
              </p>
            </div>
          </FadeInSection>
          
          <!-- Elementos decorativos -->
          <div class="mt-12 flex justify-center">
            <div class="w-24 h-px bg-gradient-to-r from-transparent via-white to-transparent opacity-50"></div>
          </div>
        </div>
      </section>
    </FadeInSection>

    <!-- Portfolio Cinematográfico -->
    <ParallaxContainer speed={0.1} intensity="high" class="py-24 bg-gradient-to-b from-gray-900 via-black to-gray-900 relative">
      <section class="max-w-7xl mx-auto px-6">
        <div class="text-center mb-20">
          <StaggerText 
            tag="h2" 
            text={`PORTFOLIO DE ${artist.name.toUpperCase()}`}
            splitBy="words"
            delay={0.3}
            stagger={0.1}
            intensity="high"
            class="text-5xl md:text-6xl font-heading text-white uppercase tracking-wider mb-8"
          />
          
          <FadeInSection delay={0.8} intensity="medium">
            <p class="text-gray-300 font-body text-xl max-w-3xl mx-auto leading-relaxed">
              Una selección de los mejores trabajos realizados por {artist.name}, mostrando su maestría en {artist.specialties.join(', ').toLowerCase()}.
            </p>
          </FadeInSection>
        </div>
        
        <!-- Grid de Portfolio Cinematográfico -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
          {artist.portfolio.map((work, index) => (
            <FadeInSection delay={1.2 + (index * 0.2)} intensity="high">
              <Card class="group overflow-hidden bg-gray-900 border border-gray-800 hover:border-gray-600 transition-all duration-500">
                <SlideInImage 
                  direction="up" 
                  delay={1.4 + (index * 0.2)} 
                  intensity="medium"
                  class="aspect-square overflow-hidden relative"
                >
                  <img 
                    src={`https://trae-api-us.mchost.guru/api/ide/v1/text_to_image?prompt=cinematic%20${work.style.toLowerCase()}%20tattoo%20${work.description.toLowerCase()}%20${work.bodyPart.toLowerCase()}%20black%20and%20white%20detailed%20professional%20dramatic%20lighting&image_size=square_hd`}
                    alt={`${work.title} - ${work.style} por ${artist.name}`}
                    class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700"
                    loading="lazy"
                  />
                  <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
                </SlideInImage>
                
                <div class="p-8">
                  <StaggerText 
                    tag="h3" 
                    text={work.title.toUpperCase()}
                    splitBy="chars"
                    delay={1.6 + (index * 0.2)}
                    stagger={0.03}
                    intensity="medium"
                    class="text-xl font-heading text-white uppercase tracking-wider mb-4"
                  />
                  
                  <div class="space-y-3 mb-6">
                    <div class="flex justify-between items-center border-b border-gray-800 pb-2">
                      <span class="text-sm font-body text-gray-400 uppercase tracking-wider">
                        Estilo
                      </span>
                      <span class="text-sm font-body text-white">
                        {work.style}
                      </span>
                    </div>
                    
                    <div class="flex justify-between items-center border-b border-gray-800 pb-2">
                      <span class="text-sm font-body text-gray-400 uppercase tracking-wider">
                        Tamaño
                      </span>
                      <span class="text-sm font-body text-white">
                        {work.size}
                      </span>
                    </div>
                    
                    <div class="flex justify-between items-center border-b border-gray-800 pb-2">
                      <span class="text-sm font-body text-gray-400 uppercase tracking-wider">
                        Ubicación
                      </span>
                      <span class="text-sm font-body text-white">
                        {work.bodyPart}
                      </span>
                    </div>
                  </div>
                  
                  <p class="text-gray-300 font-body text-sm leading-relaxed">
                    {work.description}
                  </p>
                </div>
              </Card>
            </FadeInSection>
          ))}
        </div>
      </section>
    </ParallaxContainer>

    <!-- Proceso de Trabajo Cinematográfico -->
    <FadeInSection delay={0.3} intensity="high" class="py-24 bg-gradient-to-b from-black to-gray-900">
      <section class="max-w-6xl mx-auto px-6">
        <div class="text-center">
          <StaggerText 
            tag="h2" 
            text="MI PROCESO"
            splitBy="words"
            delay={0.5}
            stagger={0.2}
            intensity="high"
            class="text-5xl md:text-6xl font-heading text-white uppercase tracking-wider mb-16"
          />
          
          <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
            <FadeInSection delay={1.0} intensity="high">
              <div class="text-center group">
                <div class="w-20 h-20 bg-gray-800 mx-auto mb-6 flex items-center justify-center text-white font-heading text-3xl border border-gray-700 group-hover:bg-gray-700 transition-all duration-300">
                  01
                </div>
                <StaggerText 
                  tag="h3" 
                  text="CONSULTA"
                  splitBy="chars"
                  delay={1.2}
                  stagger={0.05}
                  intensity="medium"
                  class="text-xl font-heading text-white uppercase tracking-wider mb-4"
                />
                <p class="text-gray-300 font-body text-sm leading-relaxed">
                  Discutimos tu idea, estilo preferido y ubicación. Analizamos referencias y definimos el concepto final.
                </p>
              </div>
            </FadeInSection>
            
            <FadeInSection delay={1.3} intensity="high">  
              <div class="text-center group">
                <div class="w-20 h-20 bg-gray-800 mx-auto mb-6 flex items-center justify-center text-white font-heading text-3xl border border-gray-700 group-hover:bg-gray-700 transition-all duration-300">
                  02
                </div>
                <StaggerText 
                  tag="h3" 
                  text="DISEÑO"
                  splitBy="chars"
                  delay={1.5}
                  stagger={0.05}
                  intensity="medium"
                  class="text-xl font-heading text-white uppercase tracking-wider mb-4"
                />
                <p class="text-gray-300 font-body text-sm leading-relaxed">
                  Creo un diseño personalizado adaptado a tu anatomía y preferencias. Refinamos hasta lograr la perfección.
                </p>
              </div>
            </FadeInSection>
            
            <FadeInSection delay={1.6} intensity="high">  
              <div class="text-center group">
                <div class="w-20 h-20 bg-gray-800 mx-auto mb-6 flex items-center justify-center text-white font-heading text-3xl border border-gray-700 group-hover:bg-gray-700 transition-all duration-300">
                  03
                </div>
                <StaggerText 
                  tag="h3" 
                  text="EJECUCIÓN"
                  splitBy="chars"
                  delay={1.8}
                  stagger={0.05}
                  intensity="medium"
                  class="text-xl font-heading text-white uppercase tracking-wider mb-4"
                />
                <p class="text-gray-300 font-body text-sm leading-relaxed">
                  Realizo el tatuaje con la máxima precisión y cuidado, asegurando tu comodidad durante todo el proceso.
                </p>
              </div>
            </FadeInSection>
          </div>
        </div>
      </section>
    </FadeInSection>

    <!-- CTA Final Cinematográfico -->
    <ParallaxContainer speed={0.1} intensity="high" class="py-24 bg-gradient-to-t from-black via-gray-900 to-black relative">
      <section class="max-w-4xl mx-auto px-6 text-center relative z-10">
        <StaggerText 
          tag="h2" 
          text="¿LISTO PARA TU PRÓXIMO TATUAJE?"
          splitBy="words"
          delay={0.3}
          stagger={0.1}
          intensity="high"
          class="text-5xl md:text-6xl font-heading text-white uppercase tracking-wider mb-8"
        />
        
        <FadeInSection delay={1.2} intensity="medium">
          <p class="text-gray-300 font-body text-xl leading-relaxed mb-10 max-w-2xl mx-auto">
            Contacta conmigo para discutir tu proyecto. Juntos crearemos una pieza única que refleje tu personalidad y estilo.
          </p>
        </FadeInSection>
        
        <FadeInSection delay={1.6} intensity="high">
          <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
            <Button 
              href={`/reservas?artist=${artist.slug}`} 
              variant="primary" 
              size="lg"
              class="transform hover:scale-105 transition-all duration-300 shadow-2xl"
            >
              Reservar con {artist.name}
            </Button>
            <Button 
              href="/artistas" 
              variant="outline" 
              size="lg"
              class="transform hover:scale-105 transition-all duration-300"
            >
              Ver Otros Artistas
            </Button>
          </div>
        </FadeInSection>
        
        <!-- Elementos decorativos cinematográficos -->
        <div class="absolute top-8 left-8 w-16 h-16 border-l-2 border-t-2 border-white opacity-10"></div>
        <div class="absolute bottom-8 right-8 w-16 h-16 border-r-2 border-b-2 border-white opacity-10"></div>
      </section>
    </ParallaxContainer>
  </main>
  
  <Footer />
</Layout>
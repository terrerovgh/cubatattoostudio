---
// src/pages/artistas/[slug].astro
// TODO: Implementar la lógica para cargar datos del artista según el slug
// import MainLayout from '../../layouts/MainLayout.astro'; // Ajustar ruta si es necesario
// import { getArtistaBySlug } from '../../data/artistas.js'; // Asumiendo que existirá esta función

// const { slug } = Astro.params;
// const artista = await getArtistaBySlug(slug); // Esto es un ejemplo, la carga de datos se definirá mejor

// if (!artista) {
//   return Astro.redirect('/404');
// }
---
<!-- <MainLayout title={artista.nombre}>
  <section class="container mx-auto py-12">
    <h1 class="text-4xl font-bold mb-6">{artista.nombre}</h1>
    <img src={artista.imagenPerfil} alt={`Foto de ${artista.nombre}`} class="w-full h-auto max-w-md mx-auto mb-6 rounded-lg shadow-lg">
    <p class="text-lg mb-4">{artista.bio}</p>
    // Más detalles del artista, galería de sus trabajos, etc.
    <h2 class="text-3xl font-semibold mt-8 mb-4">Trabajos de {artista.nombre}</h2>
    // Aquí iría una galería o listado de trabajos
  </section>
</MainLayout> -->

<div>Página de Artista: <!-- {slug} --> (Contenido Placeholder)</div>
<p>Asegúrate de descomentar y ajustar el código anterior una vez que los datos y el layout estén listos.</p>
<p>Recuerda actualizar la ruta de importación de MainLayout.astro si es necesario.</p>
<p>La importación de datos desde `../../data/artistas.js` también es un placeholder.</p>

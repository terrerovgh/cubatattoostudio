---
const { locale } = Astro.props;
---
<section id="nina" class="artist-section">
  <h2>Nina</h2>
  <p>{locale === 'en' ? 'Tattoo artist bio placeholder.' : 'Biografía de Nina.'}</p>

  <script client:load>
    import { gsap } from 'gsap';
    import { ScrollTrigger } from 'gsap/ScrollTrigger';
    gsap.registerPlugin(ScrollTrigger);
    gsap.from('#nina', {
      opacity: 0,
      y: 50,
      scrollTrigger: {
        trigger: '#nina',
        start: 'top 80%'
      }
    });
  </script>
</section>

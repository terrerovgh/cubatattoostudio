---
import SectionWrapper from '../SectionWrapper.astro';
import { existsSync } from 'fs';
import { resolve } from 'path';

interface Artist {
  name: string;
  role: string;
  image: string;
  instagram?: string;
  portfolio?: string[];
}

interface Props {
  id: string;
  backgroundImage: string;
  title?: string;
  artists?: Artist[];
}

const { id, backgroundImage, title, artists = [] } = Astro.props;

/**
 * Resolve artist profile image — prefer downloaded .jpg over .svg placeholder.
 * The fetch-insta.js script saves profile pics as /artists/{name}.jpg
 */
function resolveArtistImage(artist: Artist): string {
  // Extract the artist folder name from the image path (e.g., /artists/david.svg → david)
  const match = artist.image.match(/\/artists\/(\w+)\./);
  if (match) {
    const name = match[1];
    const jpgPath = `/artists/${name}.jpg`;
    const jpgAbsolute = resolve(process.cwd(), `public${jpgPath}`);
    if (existsSync(jpgAbsolute)) {
      return jpgPath;
    }
  }
  return artist.image; // Fallback to configured image (SVG placeholder)
}
---

<SectionWrapper id={id} backgroundImage={backgroundImage}>
  <div class="w-full max-w-5xl mx-4 sm:mx-8 lg:mx-auto space-y-12">
    {title && (
      <h2
        class="text-gradient-white text-3xl sm:text-4xl lg:text-5xl font-bold text-center tracking-[-0.02em]"
        data-animate-heading
      >
        {title}
      </h2>
    )}

    <div class="grid grid-cols-1 md:grid-cols-3 gap-5 lg:gap-6">
      {artists.map((artist) => {
        const imageSrc = resolveArtistImage(artist);
        return (
          <div
            class="liquid-glass rounded-[24px] p-8 text-center space-y-5
                   transition-all duration-500 ease-out
                   hover:border-[var(--color-glass-border-hover)]
                   hover:translate-y-[-4px]
                   hover:shadow-[0_20px_50px_rgba(0,0,0,0.4)]
                   group"
            data-animate
          >
            {/* Avatar with prismatic ring */}
            <div class="relative mx-auto w-28 h-28 sm:w-32 sm:h-32">
              <div class="absolute inset-0 rounded-full prismatic-border"></div>
              <div class="w-full h-full rounded-full overflow-hidden
                          border-2 border-white/[0.06] shadow-lg
                          transition-transform duration-500 group-hover:scale-[1.03]">
                <img
                  src={imageSrc}
                  alt={artist.name}
                  class="w-full h-full object-cover"
                  loading="lazy"
                />
              </div>
            </div>

            {/* Info */}
            <div class="space-y-1.5">
              <h3 class="text-xl font-semibold tracking-tight">{artist.name}</h3>
              <p class="text-white/40 text-sm font-light">{artist.role}</p>
            </div>

            {/* Instagram link */}
            {artist.instagram && (
              <a
                href={`https://instagram.com/${artist.instagram.replace('@', '')}`}
                target="_blank"
                rel="noopener noreferrer"
                class="inline-flex items-center gap-1.5 text-sm text-[var(--color-gold)] hover:text-[var(--color-gold-light)] transition-all duration-300"
              >
                <svg class="w-3.5 h-3.5" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 100 12.324 6.162 6.162 0 000-12.324zM12 16a4 4 0 110-8 4 4 0 010 8zm6.406-11.845a1.44 1.44 0 100 2.881 1.44 1.44 0 000-2.881z" />
                </svg>
                {artist.instagram}
              </a>
            )}
          </div>
        );
      })}
    </div>
  </div>
</SectionWrapper>

---
// Heading.astro
export interface Props {
  level?: 1 | 2 | 3 | 4 | 5 | 6;
  text: string;
}
const { level = 1, text } = Astro.props;
const Tag = `h${level}` as const;
---
<Tag>
  {text}
  <slot />
</Tag>
